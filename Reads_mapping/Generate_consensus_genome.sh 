#!/bin/bash

#SBATCH --job-name=consensus_g  # Job name

ref_genome=$1
sample_name=$2

module purge ; module load BCFtools


cat $ref_genome | bcftools consensus --missing "N" --absent "N" $sample_name.norm.flt-indels.bcf > $sample_name.consensus_genome.fna
